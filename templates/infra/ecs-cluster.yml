AWSTemplateFormatVersion: "2010-09-09"
Description: >
  dev-assistant-backend - ECS Cluster, the CloudWatch log group for the backend
  tasks and the ECS task Execution Role (pulls the image and reads the 4 SSM
  secrets). The task definition and Fargate service live in the backend-service
  stack, which imports ExecutionRoleArn and BackendLogGroupName from here.

Parameters:
  ProjectName:
    Type: String
    Default: dev-assistant
    
Resources:
  # --- ECS ---
  Cluster:
    Type: AWS::ECS::Cluster
    Properties:
      ClusterName: !Ref ProjectName

  BackendLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/ecs/${ProjectName}-backend"
      RetentionInDays: 14

  ExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${ProjectName}-ecs-execution"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy
      Policies:
        - PolicyName: read-secrets
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: ssm:GetParameters
                Resource: !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/dev-assistant/*"
              - Effect: Allow
                Action: kms:Decrypt
                Resource: "*"
                Condition:
                  StringEquals:
                    kms:ViaService: !Sub "ssm.${AWS::Region}.amazonaws.com"


Outputs:
  ClusterName:
    Description: Workflow ECS_CLUSTER variable.
    Value: !Ref Cluster
    Export:
      Name: !Sub "${ProjectName}-ClusterName"

  BackendLogGroupName:
    Description: CloudWatch Logs group for ECS tasks.
    Value: !Ref BackendLogGroup
    Export:
      Name: !Sub "${ProjectName}-BackendLogGroupName"

  ExecutionRoleArn:
    Description: ECS Task Execution Role ARN.
    Value: !GetAtt ExecutionRole.Arn
    Export:
      Name: !Sub "${ProjectName}-ExecutionRoleArn"    
